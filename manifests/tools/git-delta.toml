id = "git-delta"
category = "essentials"
tier = "core"
validation_status = "validated"
binary_names = ["delta"]
notes = "A syntax-highlighted pager for git diff, blame, and log output that improves code review readability."

[install_source]
ubuntu = "brew"
debian = "brew"
fedora = "brew"
arch = "native"
manjaro = "native"

[package_name]
arch = "git-delta"
manjaro = "git-delta"
brew = "git-delta"
