id = "helix"
category = "editor_workflow"
tier = "core"
validation_status = "validated"
binary_names = ["hx"]

[install_source]
ubuntu = "brew"
debian = "brew"
fedora = "brew"
arch = "native"
manjaro = "native"

[package_name]
arch = "helix"
manjaro = "helix"
brew = "helix"
